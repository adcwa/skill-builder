# Skill 开发规范与发布要求（skills.sh）

> 本文档是一份 **完整、可执行、可审查** 的 Skill 开发规范提示词（Requirements Prompt）。
>
> 目标：
> - 规范 Skill 的设计、实现、文档与发布
> - 确保 Skill 可以 **顺利通过 skills.sh 审核**
> - 适用于 **人类开发者 + AI Coding Agent**

---

## 1. Skill 的定位与设计原则

### 1.1 Skill 的定义

Skill 是一个 **可通过命令行调用的最小自动化能力单元**，通常用于：

- 文件处理 / 生成 / 重构
- 开发辅助（测试、文档、代码生成）
- 可重复的工程动作

Skill 的核心特征：

- **单一职责**（One Skill = One Job）
- **CLI 可调用**
- **无需 GUI**
- **可被 Claude / AI Agent 安全调用**

---

### 1.2 设计原则（强制）

1. **小而清晰**：
   - 一个 Skill 只解决一个问题
   - 不要做“工具箱型” Skill

2. **可预期行为**：
   - 输入 → 输出是确定的
   - 不允许隐式副作用

3. **安全优先**：
   - 不得默认删除、覆盖、上传数据
   - 所有危险操作必须显式确认

4. **人类可读**：
   - 所有输出应适合人类阅读
   - 错误信息必须清晰

---

## 2. Skill 仓库结构规范（强制）

### 2.1 推荐目录结构

```
skill-name/
├── README.md            # 必须：用户说明
├── skill.json           # 必须：Skill 元数据
├── index.js | main.py   # 必须：CLI 入口
├── package.json         # Node Skill 必须
├── assets/              # 可选
└── examples/            # 强烈建议
```

---

### 2.2 禁止事项

- ❌ 不得在 install 阶段执行破坏性命令
- ❌ 不得上传用户数据
- ❌ 不得依赖未声明的系统服务

---

## 3. skill.json 规范（核心）

### 3.1 最小必需字段

```json
{
  "name": "skill-name",
  "title": "Human Friendly Skill Title",
  "description": "One sentence description of what this skill does",
  "version": "0.1.0",
  "author": "your-name",
  "license": "MIT",
  "runtime": "node",
  "entry": "index.js",
  "tags": ["ai", "cli"],
  "commands": []
}
```

---

### 3.2 字段设计约束

| 字段 | 要求 |
|----|----|
| name | 小写、唯一、kebab-case |
| title | 面向人类阅读 |
| description | 不超过 120 字符 |
| runtime | node / python |
| entry | CLI 可执行文件 |
| tags | 2–5 个，准确 |

---

### 3.3 commands 规范

```json
"commands": [
  {
    "name": "generate",
    "description": "Generate output from input"
  }
]
```

- 命令名必须小写
- 必须有 description

---

## 4. CLI 行为规范（强制）

### 4.1 启动规范

- 必须支持：

```bash
skill-name <command> [options]
```

- 不允许启动后卡死
- 不允许无输出

---

### 4.2 输入输出规范

- 输入：
  - CLI 参数
  - 可选 stdin

- 输出：
  - stdout：结果
  - stderr：错误信息

---

### 4.3 错误处理

错误必须：

- 使用非 0 exit code
- 输出可理解的错误信息

示例：

```
❌ Input file not found: demo.png
```

---

## 5. README.md 规范（审核重点）

### 5.1 必须包含的章节

```md
# Skill Name

Short description.

## Install

## Usage

## Examples

## Safety

## License
```

---

### 5.2 Safety 章节（强制）

必须明确说明：

- 是否会修改文件
- 是否会覆盖输出
- 是否需要人工确认

---

## 6. 安全与合规要求（强制）

### 6.1 明确禁止

- ❌ rm -rf
- ❌ 未确认的 overwrite
- ❌ 网络数据外传

---

### 6.2 高风险操作规范

如果 Skill 涉及：

- 文件删除
- 批量修改

必须：

- 提供 dry-run
- 提供 --confirm 或交互确认

---

## 7. skills.sh 发布要求

### 7.1 发布前自检清单

- [ ] skill.json 合规
- [ ] README 完整
- [ ] CLI 可执行
- [ ] 无恶意代码

---

### 7.2 Registry 索引文件示例

```json
{
  "name": "skill-name",
  "repo": "yourname/skill-name",
  "description": "Short description",
  "runtime": "node",
  "tags": ["ai"],
  "verified": false
}
```

---

## 8. 给 AI Coding Agent 的完整提示词（可直接复制）

```
你正在开发一个可发布到 skills.sh 的 Skill。

你必须：
- 严格遵循 skill.json 与 README 规范
- Skill 必须是 CLI 工具
- 禁止危险默认行为
- 输出可预测、可审计

交付物必须包括：
1. skill.json
2. README.md
3. 可执行 CLI 实现

Skill 目标：
【在此填写】
```

---

## 9. 版本演进建议

- v0.x：实验性
- v1.0：稳定接口
- 破坏性变更必须 bump major

---

## 10. 结语

> 一个好的 Skill：
> - 让 AI 用得放心
> - 让人类一眼看懂
> - 一行命令，解决一个问题

🦞 The Lobster Way

